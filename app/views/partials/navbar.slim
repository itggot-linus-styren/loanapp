nav
	div#menu-desktop
		div#menu-header
			h1
				span.red> •
				| APTIV
				span.red •
		.search
			input#search-bar name="" placeholder=("Search loan category") type="text" value="" /
			label for="search-bar"
				i.fa.fa-search aria-hidden="true"
			button.clear-icon onclick=("document.getElementById('search-bar').value = ''") 
				i.fa.fa-times aria-hidden="true"
		.sort-by
			h2 Sort by
			.categories
				.category
					.category-checkbox
						input#c0 name="" type="checkbox" value="" /
						label for="c0" 
					label.category-name for="c0"  Name
				.category
					.category-checkbox
						input#c1 name="" type="checkbox" value="" /
						label for="c1" 
					label.category-name for="c1"  Amount
		.links
			a href="/loans" /
			h2 See all loanables
			- if defined? loanable_type
				a href="/loans/#{type}/view" /
				h2 See available #{loanable_type.loanable_name}s
				a href="/loans?type=#{type}" /
				h2 See loaned #{loanable_type.loanable_name}s
			- if ctx.session[:user_id]
				a href="/user/profile" /
				h2 Profile
		.login
			- if ctx.session[:user_id]
				a.button href="/user/logout" logout
			- else
				a.button href="/user/admin/login" login as admin
	div#menuToggle
		input type="checkbox"
		span
		span class="smaller"
		span
		div#scrim	
		ul#menu
			div class="top"
				div class="item"
					a href="/loans"
						li See all loanables
				- if defined? loanable_type
					div class="item"
						a href="/loans/#{type}/view"
							li See available #{loanable_type.loanable_name}s
					div class="item"
						a href="/loans?type=#{type}"
							li See loaned #{loanable_type.loanable_name}s
				- if ctx.session[:user_id]
					div class="item"
						a href="/user/profile"
							li Profile
			div class="bottom"
				- if ctx.session[:user_id]
					div class="item"
						a href="/user/logout"
							li Logout
				- else
					div class="item"
						a href="/user/admin/login"
							li Login admin				